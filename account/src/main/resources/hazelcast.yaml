hazelcast:
  cluster-name: account
  network:
    port:
      port-count: 10
      port: 5701
  map:
    AccountEvent_*:
      event-journal:
        enabled: true
        capacity: 100000
      indexes:
      # Sorted index on key (sequence)
        - type: SORTED
          attributes:
            - "__key"